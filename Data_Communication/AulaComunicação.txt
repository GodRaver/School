A parte de transmissão logo após a linha de abertura. Para este ficheiro o serviço é web, portanto a fase de transmissão começa pela requisição de acesso ao site na linha 4. A requisição foi feita no link disponivel no campo HOST através do metodo GET com uso do navegador que está no campo USER-AGENT, o qual é compativel com os objetos que estão no campo ACCEPT. Este cliente mantém a porta aberta por até 300ms.

A linha 38 deste ficheiro é o ultimo segmento de resposta deste servidor. As linhas que estão entre a 4 e a 38 e que envolvem as portas 3372 e 80 pertencem a fase de transmissão. No geral são linhas de envio dos segmentos do servidor para o cliente e as confirmações do cliente para o servidor. No envio de segmento do servidor para o cliente, existe uma certa quantidade de bytes que está no cabeçalho TCP no campo TCP-SEGMENT-LANE. No envio da confirmação do cliente para o servidor o campo TCP-SEGMENT-LANE tem valor igual a 0, no entanto, a flag para todos esses segmentos é do tipo ACK
Para finalizar o processo de transmissão está linha 38, em que o servidor responde com o codigo 200 de sucesso através do sistema operativo LINUX. O site transferido tem um total de 18070 Bytes contendo apenas objetos txt/html. Este servidor mantém a porta aberta por no máximo 100ms(Keep-Alive). A confirmação por parte do cliente de que recebeu a linha 38 está na linha 39, já o processo de fecho tem os flags FIN sendo neste ficheiro as linhas 40/41/42/43
